// #include <math.h>
// #include <stdio.h>
// #include <stdio.h>
// #include <stdio.h>
// #include "__hy__.c"
#include "__hy__.h"

// const double pi = 3.141593;
// #define PI 3.141593
// #define pi PI
// #define MAX(x, y) ((x) > (y) ? (x): (y))
// #define LINUX

// int ABS(const int j);
// int ABS(const int j);

// int ii;
// int ii;

int main(int argc, char *argv[], char *envp[])
{
    return 0;
}
/*
    printf("Hello, world!\n");

    printf("-1234--->>>%d.\n", ABS(-1234));


    printf("MAX(34, 56) = %d.\n", MAX(34, 56));


    int jj;

    printf("-1234--->>>%d.\n", ABS(-1234));


    for (int i = 0; i < 134; i++)
    {
        printf("错误代码：%-3d\t%s.\n", i, strerror(i));
    }


    FILE *stream;
    stream = 0;

    if (0 == (stream = fopen("11test.c_file", "r")))
    {
        printf("错误代码：%-3d\t%s", errno, strerror(errno));
    }
    else
    {
        printf("打开成功！。\n");
    }


    printf("%d --- %s\n", errno, strerror(errno));


    FILE *fp;
    fp = 0;

    if (0 == (fp = fopen("/root/test", "r")))
    {
        perror("位置27");
        printf("%d --- %s.\n", errno, strerror(errno));
    }
    if (0 == (fp = fopen("aaa", "r")))
    {
        perror("位置32");
        printf("%d --- %s.\n", errno, strerror(errno));
    }
    else
    {
        perror("位置37");
        printf("%d --- %s.\n", errno, strerror(errno));
    }
*/